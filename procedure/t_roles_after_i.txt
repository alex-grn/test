CREATE OR REPLACE FUNCTION public.t_roles_after_i (
)
RETURNS trigger AS
$body$
 declare
  res text;
 begin 
   res := P_SYSTEM_ROLEPRIVS_FILL(NEW.ID);
   return NEW;
 end;
$body$
LANGUAGE 'plpgsql'
VOLATILE
CALLED ON NULL INPUT
SECURITY INVOKER
COST 100;

ALTER FUNCTION public.t_roles_after_i ()
  OWNER TO magicbox;